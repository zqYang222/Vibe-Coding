"""Streamlit frontend for the Online Judge system (Step 6).

This is a placeholder scaffold. It will call the FastAPI backend over REST
APIs and must not read/write backend data directly.
"""

import streamlit as st

st.set_page_config(page_title="Online Judge", layout="wide")

st.title("Online Judge")
st.write("Frontend scaffold. Backend API integration comes in Step 6.")
